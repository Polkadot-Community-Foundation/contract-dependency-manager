---
"@parity/cdm-env": minor
---

Point the paseo preset at the new-generation registry (`0xc1a73a4f93fde65b1cb1680baead248073566cb0`) — the EIP-1967 proxy deployed via the CREATE3 factory and migrated from the old registry — and fix the local preset's IPFS gateway port to match PPN (8080).
